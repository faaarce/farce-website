/** Shared serif font stack — applied inline via `style` */
export const SERIF_FONT = "'PP Editorial New', Georgia, serif";

/** Tiny uppercase label */
export const lbl = "text-[9px] tracking-[2.5px] uppercase text-[#999]";

/** Standard thin rule */
export const rule = "border-b border-[#b5b0a8]";

/** Thick double-rule */
export const ruleThick = "border-b-[3px] border-double border-[#2a2a2a]";

/** Right-column border */
export const colR = "border-r border-[#b5b0a8]";

/** Justified hyphenated text */
export const just = "text-justify [hyphens:auto]";

/** Image border */
export const imgB = "border border-[#b5b0a8]";

/** Red badge (normal) */
export const badge =
  "bg-[#c0392b] text-white text-[8px] px-[5px] py-[2px] rounded-[1px] ml-1 font-extrabold tracking-[0.5px]";

/** Red badge (small) */
export const badgeSm =
  "bg-[#c0392b] text-white text-[7px] px-[5px] py-[2px] rounded-[1px] ml-1 font-extrabold";
